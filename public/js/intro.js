// Landing v2: the cold open is rendered in-page as Act I via CSS.
// This file is intentionally empty — kept as a script slot so cached HTML
// pages elsewhere on the site don't 404.
(function(){ /* noop */ })();
